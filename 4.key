### How many files and how many folders are there in the entire repo? 
There are 45 directories.
There are 168 files, not including the two new *.key files which would bring the count to 170.  171 if I add an image/logo file.

### Which commands did you run?
    find ./* -type d |wc -l # This was for the recursive count of directories
    find ./* -type f |wc -l # This was for the recursive count of files
